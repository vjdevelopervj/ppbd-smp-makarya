// This file is intentionally left empty.
// The logic has been moved to the client-side to support static export (`next export`).
// Keeping the file to avoid breaking potential imports in other parts of the app if they exist,
// but its server action functionality is no longer used for deployment.
    